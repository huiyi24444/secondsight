import 'package:flutter/material.dart';

class ProductAdditionPage extends StatelessWidget {
  const ProductAdditionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Product addition not supported on this platform.')),
    );
  }
}
